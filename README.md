# 白酒行业年报问答（RAG Demo）

从**巨潮资讯网（cninfo.com.cn）**下载 16 家白酒上市公司的年报 / 半年报**全文 PDF**，把正文与表格按行列还原成文本，
按「公司 / 章节 / 页码」切块，建立 **BM25 + 向量** 双索引，做一个**能提问、答案带出处**的网页。
附带 10 道题的逐题评测记录（召回情况、对错、错在哪）。

**在线页面**：https://xubohui-cyber.github.io/Q-A/

---

## 一、数据

16 家公司 × 3 期 = **48 份报告**，全部为巨潮资讯网披露的年度报告 / 半年度报告正文：

| 公司 | 代码 | 交易所 | 纳入报告 |
| --- | --- | --- | --- |
| 贵州茅台 | 600519 | 上交所 | 2025 年报 / 2024 年报 / 2026 半年报 |
| 五粮液 | 000858 | 深交所 | 同上 |
| 泸州老窖 | 000568 | 深交所 | 同上 |
| 洋河股份 | 002304 | 深交所 | 同上 |
| 山西汾酒 | 600809 | 上交所 | 同上 |
| 古井贡酒 | 000596 | 深交所 | 同上 |
| 今世缘 | 603369 | 上交所 | 同上 |
| 迎驾贡酒 | 603198 | 上交所 | 同上 |
| 口子窖 | 603589 | 上交所 | 同上 |
| 老白干酒 | 600559 | 上交所 | 同上 |
| 舍得酒业 | 600702 | 上交所 | 同上 |
| 水井坊 | 600779 | 上交所 | 同上 |
| 金徽酒 | 603919 | 上交所 | 同上 |
| 酒鬼酒 | 000799 | 深交所 | 同上 |
| 天佑德酒 | 002646 | 深交所 | 同上 |
| 金种子酒 | 600199 | 上交所 | 同上 |

规模：**8,254 页原文、1,011 万字、还原 12,241 张表格、切出 16,709 个文本块**。

每份报告的原始 PDF 链接、公告日期、页数、表格数、字符数、PDF 体积与哈希都记录在
[`data/manifest.json`](data/manifest.json)，可用 [`scripts/download_reports.py`](scripts/download_reports.py) 一键重下（仓库不存 PDF，控制体积）。

## 二、方法

### 1. 下载（scripts/download_reports.py + scripts/cninfo.py）
走巨潮资讯公告检索接口按「年度报告 / 半年度报告」类别拉取，取每年最新一期正文，8 线程并发下载。

### 2. 解析：文字 + 表格按行列还原（scripts/extract_text.py）
- PyMuPDF 逐页读取正文文字块；表格用 `page.find_tables()` 还原行列并转成 **Markdown 表格**；
- 落在表格区域内的文字块会被剔除，避免同一内容重复；
- 自动剔除页眉页脚：跨页重复的短行、与页号吻合的纯数字行（如 `53`、`53 / 143`）；
- 输出每份报告一个 JSON：页号 + 页面文本（表格以 `[表格开始]…[表格结束]` 内嵌）+ 表格清单 + 章节起始页。

### 3. 切块（scripts/chunk_reports.py）
- **表格块**：一张表就是一个块，行列结构完整保留；
- **正文块**：按段落聚合到 700 字左右（220–1100 字），相邻块保留一段重叠；
- 表格前的短引导语并入表格块，避免产生碎片块；
- 每块带元数据：`公司代码 / 简称 / 报告期 / 章节 / 小节 / 起始页 / 结束页 / 类型(正文|表格)`。

### 4. 索引（scripts/build_index.py）
- **BM25**：中文用「单字 + 相邻两字 bigram」，英文与数字整体成词，不依赖词典；索引时拼上章节标题；
  去掉只出现一次的词项后共 **91,055** 个词项，公式 `k1=1.5, b=0.75`；
- **向量**：`BAAI/bge-small-zh-v1.5`（ONNX int8，512 维），[CLS] 池化 + L2 归一化，浏览器端用同一模型编码问题。

### 5. 检索（scripts/retrieve.py ≈ js/rag.js，两端同一套逻辑）
- 两路分数各自 min-max 归一化后线性融合：`score = α·cos + (1−α)·BM25_norm`，默认 α=0.5；
- 问句里出现公司名 / 年份时自动收窄到对应公司、报告期；
- **查询改写**：口语说法映射成年报里的规范表述（归母净利润 → 归属于上市公司股东的净利润、分红 → 利润分配 派发现金红利…）；
- **全景题**：先把问题改写成「主要会计数据 + 指标名」，全局检索后按公司分组，保证 16 家都有可比证据，
  再用 [`js/answer.js`](js/answer.js) 的指标解析器从表里抽数排序。

### 6. 答案与出处
- 默认**抽取式答案**：从命中的原文里挑出与问题词重合度最高的句子/表格行，配 `[编号]` 出处，
  每张证据卡片给出公司、报告、章节、页码，并可一键打开巨潮资讯原文 PDF；
- 可选**大模型生成**：填入任意 OpenAI 兼容接口的 Key，把证据交给模型生成成段答案（提示词强制「只用证据、标注 [编号]、没证据就说没有」）。

## 三、评测：10 道题逐题记录

完整报告见 [`eval/rag_eval.md`](eval/rag_eval.md)，原始 JSON 见 [`eval/eval.json`](eval/eval.json)，
人工判定写在 [`eval/annotations.json`](eval/annotations.json)。

| # | 题目 | 类型 | 判定 |
| --- | --- | --- | --- |
| Q1 | 贵州茅台 2025 年营业收入和归母净利润 | 单公司 | ✅ 正确 |
| Q2 | 五粮液 2025 年酒类产品毛利率 | 单公司 | ✅ 正确 |
| Q3 | 山西汾酒 2025 年省外市场收入 | 单公司 | ❌ 错误（取了母公司口径） |
| Q4 | 泸州老窖 2026 上半年营收与净利同比 | 单公司 | ✅ 正确 |
| Q5 | 洋河股份 2025 年末合同负债 | 单公司 | ✅ 正确 |
| Q6 | 今世缘 2025 年度利润分配方案 | 单公司 | ⚠️ 部分正确（引成 2024 年度方案） |
| Q7 | 口子窖 2025 年报披露的经营风险 | 单公司 | ❌ 错误（风险章节被挤出 Top-3） |
| Q8 | **全景**：2025 年营业收入前五 | 跨公司 | ✅ 正确（16/16 家解析成功） |
| Q9 | **全景**：2025 年归母净利润增速前三 | 跨公司 | ✅ 正确 |
| Q10 | **全景**：2026 上半年哪些公司营收下滑 | 跨公司 | ✅ 正确（12 家） |
| Q11 | 贵州茅台 2026 三季度营收（语料里没有） | 拒答测试 | ❌ 未拒答 |

**7 题完全正确、1 题部分正确、3 题错误**；11 题中有 9 题的关键证据被召回。
三个典型错误（都写在报告里）：

1. **口径混淆**：汾酒省外收入，合并口径 252.02 亿元（P128）被排到第 4，第 2 位是母公司口径表的 21.8 万元，抽取式答案取了后者；
2. **章节被淹没**：口子窖的「可能面对的风险」在审计报告的「重大错报风险」等表述挤压下掉到第 6 位；
3. **不会拒答**：BM25 一定会返回若干块，缺「证据不足就明说没有」的判断。

## 四、网页

纯静态站点（GitHub Pages），**检索全部在浏览器本地完成**：

- 首屏加载 BM25 倒排索引（4.4MB gz）+ 文本块（17.8MB，CDN 自动 gzip）即可问答；
- 点「启用语义检索」再按需下载向量索引（5.3MB）与 onnxruntime wasm + bge-small-zh-v1.5 模型（约 38MB），全程不走服务端；
- 支持检索模式切换（混合 / 仅 BM25 / 仅向量）、Top-K、α 权重、公司 / 报告期过滤、跨公司全景对比表；
- 三个标签页：**问答**、**10 题评测记录**、**方法与数据**。

> 一致性校验（都在仓库里可复跑）：
> ① 浏览器端分词器与离线 Python 端**逐 token 一致**（20 条测试）；
> ② BM25 打分排序**逐条一致**（10 条查询）；
> ③ 网页端检索结果与离线评测 **Top-K 完全一致**（8/8 题）；
> ④ 浏览器内 onnxruntime-web 跑出的查询向量与 Python 端**逐位相同**（余弦 1.000000、最大分量差 0）。
>
> 校验脚本：`scripts/test_tokenizer.*`、`scripts/test_retrieval_parity.*`、`scripts/web_engine_test.mjs`。

## 五、目录结构

仓库根目录就是网页（GitHub Pages 从 `/` 发布），其余是离线管线与评测：

```
index.html css/ js/     前端：分词器 / BM25 / 向量 / 答案组装 / 界面
data/                   站点数据 + 数据清单
  meta.json               公司、报告、章节字典 + 统计
  chunks.json             16,709 个文本块（数组紧凑存储）
  bm25.bin.gz             BM25 倒排索引（varint 二进制）
  vec.i8.bin.gz           int8 量化向量（16,709 × 512）
  doclen.bin              每块词数（BM25 归一化用）
  eval.json               10 题评测结果（网页「评测记录」标签页读取）
  manifest.json           48 份报告的来源链接 / 页数 / 表格数 / 块数
model/onnx/             bge-small-zh-v1.5 int8 ONNX + tokenizer
vendor/ort/             onnxruntime-web（wasm 后端，按需加载）
scripts/                离线管线（Python）+ 一致性/端到端校验（Node）
  companies.py            16 家公司清单
  cninfo.py               巨潮资讯接口客户端
  download_reports.py     下载年报/半年报 PDF
  extract_text.py         PDF → 文字 + 表格（行列还原）
  chunk_reports.py        切块并打上公司/章节/页码元数据
  build_index.py          BM25 倒排 + bge-small-zh-v1.5 向量
  query_rewrite.py        金融口语 → 年报规范表述
  panorama.py             全景题：从「主要会计数据」表抽指标
  retrieve.py             混合检索（离线评测用）
  export_web.py           导出网页数据（含 int8 向量、varint 倒排二进制）
  eval_questions.py       跑 10 道题、记录召回、导出评测 JSON
  build_manifest.py       生成 data/manifest.json
  build_report.py         生成 eval/rag_eval.md
eval/annotations.json   人工判定（标准答案、对错、错因）
eval/rag_eval.md        10 题逐题评测报告
```

## 六、复现

```bash
pip install -r requirements.txt

python scripts/download_reports.py        # 1) 下载 48 份报告 PDF（约 175MB，可重复执行）
python scripts/extract_text.py            # 2) 解析文字与表格（多进程，约 3 分钟）
python scripts/chunk_reports.py           # 3) 切块 → data/out/chunks.jsonl
python scripts/build_index.py             # 4) BM25 + 向量（CPU 上约 8 分钟）
python scripts/export_web.py              # 5) 导出网页数据
python scripts/build_manifest.py          # 6) 生成数据清单

python scripts/eval_questions.py run      # 7) 跑 10 道题
node scripts/run_system_answers.mjs       # 8) 用网页同一份答案代码生成「系统回答」
python scripts/eval_questions.py build    # 9) 合并人工判定 → data/eval.json
python scripts/build_report.py            # 10) 生成 eval/rag_eval.md

python -m http.server 8000                # 11) 本地预览 http://127.0.0.1:8000/
```

## 七、已知问题与改进方向

- **合并 / 母公司口径混淆**：年报里同一指标常出现两套数字，字符级 BM25 分不清；应按附注编号或小节标题过滤母公司报表。
- **章节意图未利用**：问「风险 / 经营计划」时，审计报告的同类词会抢排序；可加章节过滤或章节内二次排序。
- **不会拒答**：抽取式模式缺置信度阈值，需要「证据不足就回答没有」的策略。
- **PDF 侧残余噪声**：个别表格跨页被拆、单元格换行导致表头与正文错位（如口子窖风险小节标题）；
  图像类图表（饼图/柱状图）中的数字无法解析。
- **首屏体积**：BM25 模式约 22MB、启用语义检索再加约 43MB；后续可换更小的中文向量模型或做索引裁剪。

> 语义检索需要下载 onnxruntime-web 的 wasm（约 13.6MB）与 bge-small-zh-v1.5 int8 模型（约 24MB，超过 jsDelivr 单文件 20MB 限制，
> 因此默认走同源文件）。若 GitHub Pages 访问慢，可在页面「检索设置 → 语义模型基址」里填一个自己的镜像地址
> （需包含 `tokenizer.json` 与 `onnx/model_quantized.onnx`）。

---

数据来源：巨潮资讯网（cninfo.com.cn），上交所 / 深交所披露信息。本项目仅用于课程作业与技术演示，不构成投资建议。
